from frcm.datamodel.model import Location


"""
    Currently the functions that parse data being sent to and received from external Geocoding APIs live here. 
    However if there becomes many functions and the code becomes a mess, they may be moved to their own separate files.
"""